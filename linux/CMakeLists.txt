cmake_minimum_required(VERSION 3.10)
project(runner LANGUAGES CXX)

set(BINARY_NAME "central_missoes_espaciais")
set(APPLICATION_ID "com.example.central_missoes_espaciais")

cmake_minimum_required(VERSION 3.10)
add_executable(${BINARY_NAME} main.cc)
